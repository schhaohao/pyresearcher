# 多智能体文献调研系统

基于 LangGraph 的多智能体系统，用于自动调研相关领域文献并生成综述报告。

## 功能特性

- 🤖 多智能体协作：文献搜索、摘要提取、内容分析、报告生成
- 📊 智能文献管理：自动收集、分类和整理文献
- 📝 自动生成综述：基于收集的文献自动生成结构化综述报告
- 🔄 基于 LangGraph：使用状态图管理智能体工作流

## 项目结构

```
researcher_demo/
├── researcher/              # 主包目录
│   ├── __init__.py
│   ├── __main__.py         # 命令行入口
│   ├── main.py             # 主程序入口
│   ├── agents/             # 智能体定义
│   │   ├── __init__.py
│   │   ├── searcher.py     # 文献搜索智能体
│   │   ├── analyzer.py     # 内容分析智能体
│   │   ├── summarizer.py   # 摘要生成智能体
│   │   └── writer.py       # 报告撰写智能体
│   ├── graph/              # LangGraph 图定义
│   │   ├── __init__.py
│   │   └── research_graph.py
│   ├── tools/              # 工具函数
│   │   ├── __init__.py
│   │   ├── search_tools.py # 文献搜索工具
│   │   ├── paper_tools.py  # 论文处理工具
│   │   └── report_tools.py # 报告生成工具
│   ├── utils/              # 工具函数
│   │   ├── __init__.py
│   │   ├── logger.py       # 日志工具
│   │   └── file_utils.py   # 文件操作工具
│   ├── config/             # 配置管理
│   │   ├── __init__.py
│   │   └── settings.py     # 配置设置
│   └── prompts/            # 提示词模板
│       ├── __init__.py
│       ├── search_prompts.py
│       ├── analysis_prompts.py
│       └── writing_prompts.py
├── tests/                  # 测试文件
│   ├── __init__.py
│   └── test_agents.py
├── examples/               # 示例文件
│   └── example_usage.py
├── outputs/                # 输出目录
│   ├── papers/             # 下载的论文
│   └── reports/            # 生成的报告
├── requirements.txt        # 依赖包
├── setup.py               # 安装配置
├── .env.example           # 环境变量示例
├── .gitignore            # Git 忽略文件
└── README.md             # 项目说明

```

## 安装

```bash
# 克隆项目
cd researcher_demo

# 安装依赖
pip install -r requirements.txt

# 或使用开发模式安装
pip install -e .
```

## 配置

1. 复制环境变量示例文件：
```bash
cp .env.example .env
```

2. 编辑 `.env` 文件，填入你的 API keys：
```
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
# 其他 API keys...
```

## 使用方法

```python
from researcher.main import ResearchAgent

# 创建调研智能体
agent = ResearchAgent()

# 执行文献调研
result = agent.research(
    topic="大语言模型在代码生成中的应用",
    max_papers=10
)

# 生成综述报告
report = agent.generate_report(result)
```

## 开发

```bash
# 运行测试
pytest tests/

# 运行示例
python examples/example_usage.py
```

## 许可证

MIT License

