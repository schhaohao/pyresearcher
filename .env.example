# ============================================
# 多智能体文献调研系统 - 环境变量配置示例
# ============================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填入你的实际配置值
# 3. 不要将 .env 文件提交到版本控制系统

# ============================================
# API Keys（至少需要配置一个）
# ============================================
# OpenAI API Key（用于 GPT 模型）
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key（可选，用于 Claude 模型）
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# DeepSeek API Key（可选，用于 DeepSeek 模型）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# SiliconFlow API Key（可选，用于 SiliconFlow 模型）
SILICONFLOW_API_KEY=${MY_SILICONFLOW}

# ============================================
# 默认提供商配置
# ============================================
# 默认使用的 LLM 提供商（openai, anthropic, deepseek, siliconflow）
DEFAULT_PROVIDER=openai

# ============================================
# 模型配置
# ============================================
# 默认使用的语言模型（会被各厂商的默认模型覆盖）
DEFAULT_MODEL=gpt-4

# 模型温度参数（0.0-1.0，越高越随机）
TEMPERATURE=0.7

# 最大生成 token 数
MAX_TOKENS=4000

# ============================================
# 各厂商的默认模型
# ============================================
# OpenAI 默认模型
OPENAI_MODEL=gpt-4

# Anthropic 默认模型
ANTHROPIC_MODEL=claude-3-sonnet-20240229

# DeepSeek 默认模型
DEEPSEEK_MODEL=deepseek-chat

# SiliconFlow 默认模型
SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V3.2-Exp

# ============================================
# Elasticsearch 配置
# ============================================
# 是否启用 Elasticsearch 知识库（true/false）
ES_ENABLED=false

# Elasticsearch 地址
ES_HOST=localhost
ES_PORT=9200
ES_SCHEME=http

# 知识库索引名称
ES_INDEX=researcher-knowledge-base

# 嵌入模型（用于生成向量，如 OpenAI text-embedding 模型或自建模型）
ES_EMBEDDING_MODEL=text-embedding-3-large

# 如果需要鉴权，请填写用户名、密码
ES_USERNAME=
ES_PASSWORD=

# 可选：CA 证书路径（https 自签证书时使用）
ES_CA_CERT_PATH=

# ============================================
# Embedding 服务配置
# ============================================
EMBEDDING_SERVICE_BASE_URL=http://10.195.28.136:8000/v1
EMBEDDING_SERVICE_API_KEY=sk-dummy
EMBEDDING_SERVICE_MODEL_NAME=bge-large-zh-v1.5

# ============================================
# 搜索配置
# ============================================
# 默认搜索的最大论文数量
MAX_PAPERS=10

# 默认使用的搜索引擎（arxiv, semantic_scholar 等）
DEFAULT_SEARCH_ENGINE=arxiv

# ============================================
# 输出配置
# ============================================
# 输出根目录
OUTPUT_DIR=./outputs

# 论文存储目录
PAPERS_DIR=./outputs/papers

# 报告存储目录
REPORTS_DIR=./outputs/reports

# ============================================
# 日志配置
# ============================================
# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# 日志文件路径（可选，留空则只输出到控制台）
LOG_FILE=
